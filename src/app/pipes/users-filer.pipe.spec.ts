import { UsersFilerPipe } from './users-filer.pipe';

describe('UsersFilerPipe', () => {
  it('create an instance', () => {
    const pipe = new UsersFilerPipe();
    expect(pipe).toBeTruthy();
  });
});
